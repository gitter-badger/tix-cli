#!/bin/bash
#
# Clones the server module

set -e

git_url="https://$(github-token)@github.com/TixInc/server"
module_path=~/tixinc/server

>&2 echo cloning $git_url to $module_path...

git clone $git_url $module_path

>&2 echo server cloned successfully.