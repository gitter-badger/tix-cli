#!/bin/bash
#
# Clones the server module

token=$(github-token)
echo token val: $token

#gitUrl="https://$(github-token)@github.com/TixInc/server"
installPath=~/tixinc

#echo gitURL: $gitUrl
#git clone $gitUrl $installPath